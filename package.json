{
  "name": "api-tarefas",
  "version": "1.0.0",
  "scripts": {
    "start": "./node_modules/.bin/json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "engines": {
    "node": "18.x"
  }
}