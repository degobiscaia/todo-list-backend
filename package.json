{
  "name": "api-tarefas",
  "version": "1.0.0",
  "scripts": {
    "start": "./node_modules/.bin/json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
